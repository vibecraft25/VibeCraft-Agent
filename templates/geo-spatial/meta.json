{
  "type": "geo-spatial",
  "name": "Geo-Spatial Visualization",
  "description": "지리적 데이터를 지도에 표시하는 기본 시각화",
  "requiredLibraries": ["react-leaflet", "leaflet"],
  "systemPrompt": "Create a simple geo-spatial visualization with basic interactive map and markers.",
  "componentStructure": {
    "mainComponents": [
      {
        "name": "SimpleMap",
        "description": "Basic Leaflet map component with markers",
        "props": [
          { "name": "center", "type": "[number, number]" },
          { "name": "zoom", "type": "number" },
          { "name": "markers", "type": "MapMarker[]" }
        ],
        "features": ["markers", "popups", "zoom"]
      }
    ],
    "hooks": [
      {
        "name": "useMapData",
        "description": "Hook for fetching and processing geo data"
      }
    ],
    "utils": [
      {
        "name": "geoUtils",
        "description": "Convert region names to coordinates"
      }
    ]
  },
  "dataProcessingPatterns": [
    {
      "name": "Location Query",
      "sqlTemplate": "SELECT region, COUNT(*) as count FROM {{table}} GROUP BY region",
      "dataTransformation": "Map region names to coordinates and display as markers"
    }
  ]
}